<?php
/**
 * ROLE 4 — PLATFORM ADMIN: CuisineController
 * PDF features:
 * - Manage platform-level cuisine categories used to classify restaurants
 */

require_once dirname(__DIR__, 3) . '/includes/bootstrap.php';
requireRole(['admin']);

// TODO: index(), create(), update(), delete() — store in platform_settings or separate table if extended
