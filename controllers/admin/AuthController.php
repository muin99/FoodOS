<?php
/**
 * ROLE 4 — PLATFORM ADMIN: AuthController
 * PDF features:
 * - Log in to dedicated admin dashboard (no public registration)
 */

require_once dirname(__DIR__, 3) . '/includes/bootstrap.php';
// requireRole(['admin']); — call only on protected actions, NOT on login

// TODO: login(), logout()
