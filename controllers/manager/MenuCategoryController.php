<?php
/**
 * ROLE 2 — RESTAURANT MANAGER: MenuCategoryController
 * PDF features:
 * - Manage menu categories: create, rename, reorder, delete
 */

require_once dirname(__DIR__, 3) . '/includes/bootstrap.php';
requireRole(['manager']);

// TODO: index(), create(), rename(), reorder(), delete()
