<?php
/**
 * ROLE 2 — RESTAURANT MANAGER: MenuItemController
 * PDF features:
 * - Add items: name, description, price, image, category, availability toggle
 * - Edit and delete existing items
 */

require_once dirname(__DIR__, 3) . '/includes/bootstrap.php';
requireRole(['manager']);

// TODO: index(), create(), edit(), delete(), toggleAvailability()
