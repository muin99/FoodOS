<?php
/**
 * MODEL: restaurants
 * Table: id, manager_id, name, description, cuisine_type, address, city, logo_path,
 *        opening_hours, delivery_radius_km, is_open, is_approved, created_at
 * Customer: browse approved restaurants, search/filter, detail page, favourites.
 * Manager: profile CRUD, open/closed toggle.
 * Admin: approve/reject/suspend restaurants, featured list.
 */

class RestaurantModel
{
    // TODO: getApproved, search, getById, create, update, setApproval, etc.
}
