/**
 * ROLE 1 — CUSTOMER (AJAX)
 * PDF: Poll order status without full page reload (XMLHttpRequest).
 * Endpoint: /FoodOS/api/customer/order_status.php?order_id=
 */
// TODO: setInterval poll — update status badge in DOM from JSON response
